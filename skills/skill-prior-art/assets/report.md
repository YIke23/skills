<!--
先行事例調査レポートの雛形。複製して埋める。

    cp ${CLAUDE_SKILL_DIR}/assets/report.md docs/prior-art-<対象>.md

見出しの文言と順序は check_report.py が照合する。**見出しを変えない。**
埋め終わったら必ず通す。

    python3 ${CLAUDE_SKILL_DIR}/scripts/check_report.py <このファイルのパス>

終了コード 0=問題なし / 1=警告あり / 2=不適合。**2 が出たまま SKILL.md を書かない。**
この雛形を埋めずにそのまま検証すると、当然 2 で落ちる。それが正しい挙動。

各見出しにどの検査が掛かるかは、見出しの直下のコメントに書いてある。
コメント（この <!-- --> の中）は検査の対象外なので、消しても残してもよい。
-->

# 先行事例調査レポート: <対象>

## 調査日

<!-- 検査: 今日の日付でなければ警告。YYYY-MM-DD で書く。
     学習時点の記憶ではなく、今日取得した情報で書いたことの合図。 -->

YYYY-MM-DD

## 何を探したか

<!-- Step 0。「何をする / いつ発動する / どのサービス・技術に触る」を1文で。 -->

### 検索語

<!-- 検査: 英語が3個未満、または日本語が0個で警告。
     英語が主戦場なので英語を先に作る。区切りは読点でもカンマでもよい。 -->

- 英語:
- 日本語:

## 調査を省略した理由

<!-- 通常は「なし」のまま。
     ユーザーが明示的に「調査はいらない、そのまま作って」と言った場合だけ、
     その発言を引用して書く（SKILL.md の唯一の例外）。
     書いてあると T0〜T2 の検査を飛ばし、警告(exit 1)で通る。
     黙って飛ばすのではなく、記録と引き換えに通す。 -->

なし

## 調査した層

<!-- 検査（不適合）: T0 / T1 / T2 は節があり、中身が空でないこと。
     検査（不適合）: 「該当なし」と書く層には、実行したクエリを必ず併記すること。
                    クエリの無い「該当なし」は未調査と同じ。
     検査（警告）  : T3 / T4 / T5 が空。上で見つかったなら軽く流してよい。 -->

### T0 — 手元

実行したクエリ:

```bash
bash ${CLAUDE_SKILL_DIR}/scripts/search_local.sh <検索語> <検索語>
```

結果:

### T1 — Anthropic公式

実行したクエリ:

```bash
gh search code --repo anthropics/skills --filename SKILL.md <検索語>
gh api repos/anthropics/claude-plugins-official --jq .pushed_at
```

結果:

### T2 — ベンダー公式

<!-- 最も見落とされ、最も効く層。作業対象のサービス自身が公式スキルを出していないか。
     探し方は references/sources.md の T2 を読む。 -->

実行したクエリ:

```bash
gh search repos --owner <vendor> skill --limit 10
```

結果:

### T3 — マーケットプレイス

実行したクエリ:

結果:

### T4 — skills.sh

実行したクエリ:

結果:

### T5 — 著名企業・個人

実行したクエリ:

結果:

## 候補

<!-- 候補ごとに ### を1つ。候補が0件なら、この節は「なし」とだけ書く。

     検査（不適合）: 出典URLの無い数値。★数・インストール数・「N件」などを書く行には、
                    同じ行に http(s):// の取得元を置く。表なら同じ行に。
     検査（不適合）: 結論が adopt / vendor / derive のとき、採用元の候補の「監査結果」が
                    空だと落ちる（層が T0 の場合を除く）。監査の観点は
                    references/sources.md の末尾。
     検査（警告）  : 「足りないこと」が空。adopt なら空でも筋は通るが、
                    derive / build-new を選ぶなら書けないとおかしい。 -->

### <候補名>

- 層: T4
- 提供元:
- 取得元URL:
- できること:
- 足りないこと:
- 保守状況:
- 監査結果:

## 結論

<!-- 検査（不適合）: adopt / vendor / derive / build-new のどれか1語。空欄も他の語も落ちる。
     検査（不適合）: 採用元。adopt / vendor / derive なら「## 候補」にある候補名を書く。
                    build-new なら「なし」。SKILL.md Step 3 が derive に借用元の明記を
                    求めているので、機械側でも要求する。 -->

build-new

- 採用元: なし

### 既存で満たせない点

<!-- 検査（不適合）: 結論が derive / build-new のとき、ここが1件も無いと落ちる。
     書けないなら答えは adopt か vendor。 -->

-
